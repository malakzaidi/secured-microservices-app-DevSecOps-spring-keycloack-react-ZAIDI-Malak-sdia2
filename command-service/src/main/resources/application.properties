spring.application.name=command-service

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/commanddb
spring.datasource.username=commanduser
spring.datasource.password=commandpass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Server Port
server.port=8082

# Logging
logging.level.org.security.commandservice=DEBUG
